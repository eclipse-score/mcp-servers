# APM package template

Copy this directory to add a new APM package. Edit `apm.yml`, then place
instructions, prompts, agents, skills, hooks, or context fragments below the
corresponding `.apm/<type>/` directory.
