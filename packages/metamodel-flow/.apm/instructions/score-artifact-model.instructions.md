<!-- SPDX-License-Identifier: Apache-2.0 -->
<!-- Copyright (c) 2026 Contributors to the Eclipse Foundation -->

---
name: score-artifact-model
description: Normative S-CORE artifact model — need types, options, links and graph rules
applyTo: "**/*.rst"
---

# S-CORE Artifact Model

> Generated from the `docs-as-code` metamodel projection. Do not edit this file by hand.
> Regenerate with `python src/render_markdown.py --write`.
> Metamodel digest: `sha256:90caf4d5e869a5a767044b4400f4b691ed7687856cb2acedd65dd9fb3d06c1d1`.

## Base options

- `source_code_link` (optional): `^https://github.com/.*`
- `testlink` (optional): `^https://github.com/.*`
- `version` (mandatory): `^[0-9]+$`

## Need types

### `aou_req` — Assumption of Use Requirement
Tags: `requirement`, `requirement_excl_process`
Parts: `3`

**Mandatory options**
- `content` (mandatory): `^[\s\S]+$`
- `reqtype` (mandatory): `^(Functional|Interface|Process|Non-Functional)$`
- `safety` (mandatory): `^(QM|ASIL_B)$`
- `security` (mandatory): `^(YES|NO)$`
- `status` (mandatory): `^(valid|invalid)$`
- `version` (mandatory): `^[0-9]+$`

**Optional options**
- `hash` (optional): `^.*$`
- `source_code_link` (optional): `^https://github.com/.*`
- `testcovered` (optional): `^(YES|NO)$`
- `testlink` (optional): `^https://github.com/.*`

### `assertion` — Assertion
Parts: `3`

**Mandatory options**
- `id` (mandatory): `^assertion__[0-9a-zA-Z_-]*$`
- `status` (mandatory): `^(draft|valid)$`
- `version` (mandatory): `^[0-9]+$`

**Optional options**
- `source_code_link` (optional): `^https://github.com/.*`
- `testlink` (optional): `^https://github.com/.*`

**Optional links**
- `links` (optional; targets: any target (`*`))

### `comp` — Component
Tags: `architecture_element`
Parts: `2`

**Mandatory options**
- `safety` (mandatory): `^(QM|ASIL_B)$`
- `security` (mandatory): `^(YES|NO)$`
- `status` (mandatory): `^(valid|invalid)$`
- `version` (mandatory): `^[0-9]+$`

**Mandatory links**
- `belongs_to` (mandatory; targets: `feat`)

**Optional options**
- `source_code_link` (optional): `^https://github.com/.*`
- `testlink` (optional): `^https://github.com/.*`

**Optional links**
- `consists_of` (optional; targets: `comp`)
- `fulfils` (optional; targets: `aou_req`)
- `implements` (optional; targets: `logic_arc_int`, `real_arc_int_op`)
- `uses` (optional; targets: `logic_arc_int`, `real_arc_int_op`)

### `comp_arc_dyn` — Component Sequence Diagram
Tags: `architecture_view`
Parts: `3`

**Mandatory options**
- `safety` (mandatory): `^(QM|ASIL_B)$`
- `security` (mandatory): `^(YES|NO)$`
- `status` (mandatory): `^(valid|invalid)$`
- `version` (mandatory): `^[0-9]+$`

**Mandatory links**
- `belongs_to` (mandatory; targets: `comp`)

**Optional options**
- `source_code_link` (optional): `^https://github.com/.*`
- `testlink` (optional): `^https://github.com/.*`

**Optional links**
- `fulfils` (optional; targets: `comp_req`)

### `comp_arc_sta` — Component Package Diagram
Tags: `architecture_view`
Parts: `3`

**Mandatory options**
- `safety` (mandatory): `^(QM|ASIL_B)$`
- `security` (mandatory): `^(YES|NO)$`
- `status` (mandatory): `^(valid|invalid)$`
- `version` (mandatory): `^[0-9]+$`

**Mandatory links**
- `belongs_to` (mandatory; targets: `comp`)

**Optional options**
- `source_code_link` (optional): `^https://github.com/.*`
- `testlink` (optional): `^https://github.com/.*`

**Optional links**
- `fulfils` (optional; targets: `comp_req`)
- `uses` (optional; targets: `logic_arc_int`, `real_arc_int_op`)

### `comp_req` — Component Requirement
Tags: `requirement`, `requirement_excl_process`
Parts: `3`

**Mandatory options**
- `content` (mandatory): `^[\s\S]+$`
- `reqtype` (mandatory): `^(Functional|Interface|Process|Non-Functional)$`
- `safety` (mandatory): `^(QM|ASIL_B)$`
- `security` (mandatory): `^(YES|NO)$`
- `status` (mandatory): `^(valid|invalid)$`
- `version` (mandatory): `^[0-9]+$`

**Mandatory links**
- `satisfied_by` (mandatory; targets: `comp`)

**Optional options**
- `hash` (optional): `^.*$`
- `source_code_link` (optional): `^https://github.com/.*`
- `testcovered` (optional): `^(YES|NO)$`
- `testlink` (optional): `^https://github.com/.*`

**Optional links**
- `covers` (optional; targets: `aou_req`)
- `derived_from` (optional; targets: `feat_req`)

### `comp_saf_dfa` — Component DFA (Dependent Failure Analysis)
Tags: `dependent_failure_analysis`, `safety_analysis`
Parts: `3`

**Mandatory options**
- `content` (mandatory): `^[\s\S]+$`
- `failure_effect` (mandatory): `^.+$`
- `failure_id` (mandatory): `^.+$`
- `status` (mandatory): `^(valid|invalid)$`
- `sufficient` (mandatory): `^(yes|no)$`
- `version` (mandatory): `^[0-9]+$`

**Mandatory links**
- `violates` (mandatory; targets: `comp_arc_sta`)

**Optional options**
- `mitigation_issue` (optional): `^https://github\.com/[^/]+/[^/]+/issues/\d+$`
- `safety_relevant` (optional): `^(yes|no)$`
- `source_code_link` (optional): `^https://github.com/.*`
- `testlink` (optional): `^https://github.com/.*`

**Optional links**
- `mitigated_by` (optional; targets: `aou_req`, `comp_req`)

### `comp_saf_fmea` — Component FMEA (Failure Mode and Effects Analysis)
Tags: `failure_mode_effects_analysis`, `safety_analysis`
Parts: `3`

**Mandatory options**
- `content` (mandatory): `^[\s\S]+$`
- `failure_effect` (mandatory): `^.+$`
- `fault_id` (mandatory): `^.+$`
- `status` (mandatory): `^(valid|invalid)$`
- `sufficient` (mandatory): `^(yes|no)$`
- `version` (mandatory): `^[0-9]+$`

**Mandatory links**
- `violates` (mandatory; targets: `comp_arc_dyn`, `comp_arc_sta`)

**Optional options**
- `mitigation_issue` (optional): `^https://github\.com/[^/]+/[^/]+/issues/\d+$`
- `root_cause` (optional): `^[\s\S]+$`
- `safety_relevant` (optional): `^(yes|no)$`
- `source_code_link` (optional): `^https://github.com/.*`
- `testlink` (optional): `^https://github.com/.*`

**Optional links**
- `mitigated_by` (optional; targets: `aou_req`, `comp_req`)

### `comp_sec_ana` — Component Security Analysis (Threat Scenario)
Tags: `security_analysis`, `threat_scenario`
Parts: `3`

**Mandatory options**
- `content` (mandatory): `^[\s\S]+$`
- `status` (mandatory): `^(valid|invalid)$`
- `sufficient` (mandatory): `^(yes|no)$`
- `threat_effect` (mandatory): `^.+$`
- `threat_scenario_id` (mandatory): `^(AS_01_(01|02|04|05|06|07|09|10)|CO_01_0[1-7]|SC_01_0[2-5]|SI_01_0[2-5]|UI_01_(0[1-9]|1[0-2]))$`
- `version` (mandatory): `^[0-9]+$`

**Optional options**
- `mitigation_issue` (optional): `^https://github\.com/[^/]+/[^/]+/issues/\d+$`
- `source_code_link` (optional): `^https://github.com/.*`
- `testlink` (optional): `^https://github.com/.*`

**Optional links**
- `mitigated_by` (optional; targets: `aou_req`, `comp_req`)

### `comp_sec_threat` — Component Security Analysis Threat (STRIDE)
Tags: `security_analysis`, `stride_threat`
Parts: `3`

**Mandatory options**
- `content` (mandatory): `^[\s\S]+$`
- `status` (mandatory): `^(valid|invalid)$`
- `threat_id` (mandatory): `^(AU_01_0[1-3]|AZ_01_0[1-3]|CT_01_0[1-2]|DS_01_0[1-3]|EX_01_0[1-6]|LA_01_0[1-3]|MT_01_0[1-7])$`
- `version` (mandatory): `^[0-9]+$`

**Optional options**
- `source_code_link` (optional): `^https://github.com/.*`
- `testlink` (optional): `^https://github.com/.*`

### `dec_rec` — Decision Record

**Mandatory options**
- `context` (mandatory): `^.+$`
- `decision` (mandatory): `^.+$`
- `status` (mandatory): `^(proposed|accepted|deprecated|rejected|superseded)$`
- `version` (mandatory): `^[0-9]+$`

**Optional options**
- `consequences` (optional): `^.*$`
- `source_code_link` (optional): `^https://github.com/.*`
- `testlink` (optional): `^https://github.com/.*`
- `tracking` (optional): `^https://github\.com/[^/]+/[^/]+/issues/\d+$`

**Optional links**
- `affects` (optional; targets: any target (`*`))

### `doc_concept` — Concept Definition
Parts: `2`

**Mandatory options**
- `status` (mandatory): `^(valid|draft)$`
- `version` (mandatory): `^[0-9]+$`

**Optional options**
- `source_code_link` (optional): `^https://github.com/.*`
- `testlink` (optional): `^https://github.com/.*`

### `doc_getstrt` — Getting Started
Parts: `2`

**Mandatory options**
- `status` (mandatory): `^(valid|draft)$`
- `version` (mandatory): `^[0-9]+$`

**Optional options**
- `source_code_link` (optional): `^https://github.com/.*`
- `testlink` (optional): `^https://github.com/.*`

### `doc_tool` — Tool Verification Report
Parts: `2`

**Mandatory options**
- `safety_affected` (mandatory): `^(YES|NO)$`
- `security_affected` (mandatory): `^(YES|NO)$`
- `status` (mandatory): `^(draft|evaluated|qualified|released|rejected)$`
- `tcl` (mandatory): `^(LOW|HIGH)$`
- `version` (mandatory): `^[0-9]+$`

**Optional options**
- `approver` (optional): `^.*$`
- `author` (optional): `^.*$`
- `reviewer` (optional): `^.*$`
- `source_code_link` (optional): `^https://github.com/.*`
- `testlink` (optional): `^https://github.com/.*`
- `tool_version` (optional): `^.*$`

**Optional links**
- `realizes` (optional; targets: `workproduct`)

### `document` — Generic Document
Prefix: `doc__`
Parts: `2`

**Mandatory options**
- `safety` (mandatory): `^(QM|ASIL_B)$`
- `security` (mandatory): `^(YES|NO)$`
- `status` (mandatory): `^(valid|draft|invalid)$`
- `version` (mandatory): `^[0-9]+$`

**Mandatory links**
- `realizes` (mandatory; targets: `workproduct`)

**Optional options**
- `approver` (optional): `^.*$`
- `author` (optional): `^.*$`
- `reviewer` (optional): `^.*$`
- `source_code_link` (optional): `^https://github.com/.*`
- `testlink` (optional): `^https://github.com/.*`

### `feat` — Feature
Tags: `architecture_element`
Parts: `2`

**Mandatory options**
- `safety` (mandatory): `^(QM|ASIL_B)$`
- `security` (mandatory): `^(YES|NO)$`
- `status` (mandatory): `^(valid|invalid)$`
- `version` (mandatory): `^[0-9]+$`

**Optional options**
- `source_code_link` (optional): `^https://github.com/.*`
- `testlink` (optional): `^https://github.com/.*`

**Optional links**
- `includes` (optional; targets: `logic_arc_int`, `logic_arc_int_op`)
- `provides` (optional; targets: `logic_arc_int`, `logic_arc_int_op`)
- `uses` (optional; targets: `logic_arc_int`, `logic_arc_int_op`)

### `feat_arc_dyn` — Feature Sequence Diagram
Tags: `architecture_element`, `architecture_view`
Parts: `3`

**Mandatory options**
- `safety` (mandatory): `^(QM|ASIL_B)$`
- `security` (mandatory): `^(YES|NO)$`
- `status` (mandatory): `^(valid|invalid)$`
- `version` (mandatory): `^[0-9]+$`

**Mandatory links**
- `belongs_to` (mandatory; targets: `feat`)

**Optional options**
- `source_code_link` (optional): `^https://github.com/.*`
- `testlink` (optional): `^https://github.com/.*`

**Optional links**
- `fulfils` (optional; targets: `feat_req`)

### `feat_arc_sta` — Feature & Feature Package Diagram
Tags: `architecture_element`, `architecture_view`
Parts: `3`

**Mandatory options**
- `safety` (mandatory): `^(QM|ASIL_B)$`
- `security` (mandatory): `^(YES|NO)$`
- `status` (mandatory): `^(valid|invalid)$`
- `version` (mandatory): `^[0-9]+$`

**Mandatory links**
- `belongs_to` (mandatory; targets: `feat`)
- `includes` (mandatory; targets: `logic_arc_int`, `logic_arc_int_op`)

**Optional options**
- `source_code_link` (optional): `^https://github.com/.*`
- `testlink` (optional): `^https://github.com/.*`

**Optional links**
- `fulfils` (optional; targets: `aou_req`, `feat_req`)

### `feat_req` — Feature Requirement
Tags: `requirement`, `requirement_excl_process`
Parts: `3`

**Mandatory options**
- `content` (mandatory): `^[\s\S]+$`
- `reqtype` (mandatory): `^(Functional|Interface|Process|Non-Functional)$`
- `safety` (mandatory): `^(QM|ASIL_B)$`
- `security` (mandatory): `^(YES|NO)$`
- `status` (mandatory): `^(valid|invalid)$`
- `valid_from` (mandatory): `^v(0|[1-9]\d*)\.(0|[1-9]\d*)(\.(0|[1-9]\d*))?$`
- `version` (mandatory): `^[0-9]+$`

**Mandatory links**
- `satisfied_by` (mandatory; targets: `feat`)

**Optional options**
- `hash` (optional): `^.*$`
- `source_code_link` (optional): `^https://github.com/.*`
- `testcovered` (optional): `^(YES|NO)$`
- `testlink` (optional): `^https://github.com/.*`
- `valid_until` (optional): `^v(0|[1-9]\d*)\.(0|[1-9]\d*)(\.(0|[1-9]\d*))?$`

**Optional links**
- `covers` (optional; targets: `aou_req`)
- `derived_from` (optional; targets: `stkh_req`)

### `feat_saf_dfa` — Feature DFA (Dependent Failure Analysis)
Tags: `dependent_failure_analysis`, `safety_analysis`
Parts: `3`

**Mandatory options**
- `content` (mandatory): `^[\s\S]+$`
- `failure_effect` (mandatory): `^.+$`
- `failure_id` (mandatory): `^.+$`
- `status` (mandatory): `^(valid|invalid)$`
- `sufficient` (mandatory): `^(yes|no)$`
- `version` (mandatory): `^[0-9]+$`

**Mandatory links**
- `violates` (mandatory; targets: `feat_arc_sta`)

**Optional options**
- `mitigation_issue` (optional): `^https://github\.com/[^/]+/[^/]+/issues/\d+$`
- `safety_relevant` (optional): `^(yes|no)$`
- `source_code_link` (optional): `^https://github.com/.*`
- `testlink` (optional): `^https://github.com/.*`

**Optional links**
- `mitigated_by` (optional; targets: `aou_req`, `feat_req`)

### `feat_saf_fmea` — Feature FMEA (Failure Mode and Effects Analysis)
Tags: `failure_mode_effects_analysis`, `safety_analysis`
Parts: `3`

**Mandatory options**
- `content` (mandatory): `^[\s\S]+$`
- `failure_effect` (mandatory): `^.+$`
- `fault_id` (mandatory): `^.+$`
- `status` (mandatory): `^(valid|invalid)$`
- `sufficient` (mandatory): `^(yes|no)$`
- `version` (mandatory): `^[0-9]+$`

**Mandatory links**
- `violates` (mandatory; targets: `feat_arc_dyn`, `feat_arc_sta`)

**Optional options**
- `mitigation_issue` (optional): `^https://github\.com/[^/]+/[^/]+/issues/\d+$`
- `root_cause` (optional): `^[\s\S]+$`
- `safety_relevant` (optional): `^(yes|no)$`
- `source_code_link` (optional): `^https://github.com/.*`
- `testlink` (optional): `^https://github.com/.*`

**Optional links**
- `mitigated_by` (optional; targets: `aou_req`, `feat_req`)

### `feat_sec_ana` — Feature Security Analysis (Threat Scenario)
Tags: `security_analysis`, `threat_scenario`
Parts: `3`

**Mandatory options**
- `content` (mandatory): `^[\s\S]+$`
- `status` (mandatory): `^(valid|invalid)$`
- `sufficient` (mandatory): `^(yes|no)$`
- `threat_effect` (mandatory): `^.+$`
- `threat_scenario_id` (mandatory): `^(AS_01_(01|02|04|05|06|07|09|10)|CO_01_0[1-7]|SC_01_0[2-5]|SI_01_0[2-5]|UI_01_(0[1-9]|1[0-2]))$`
- `version` (mandatory): `^[0-9]+$`

**Optional options**
- `mitigation_issue` (optional): `^https://github\.com/[^/]+/[^/]+/issues/\d+$`
- `source_code_link` (optional): `^https://github.com/.*`
- `testlink` (optional): `^https://github.com/.*`

**Optional links**
- `mitigated_by` (optional; targets: `aou_req`, `feat_req`)

### `feat_sec_threat` — Feature Security Analysis Threat (STRIDE)
Tags: `security_analysis`, `stride_threat`
Parts: `3`

**Mandatory options**
- `content` (mandatory): `^[\s\S]+$`
- `status` (mandatory): `^(valid|invalid)$`
- `threat_id` (mandatory): `^(AU_01_0[1-3]|AZ_01_0[1-3]|CT_01_0[1-2]|DS_01_0[1-3]|EX_01_0[1-6]|LA_01_0[1-3]|MT_01_0[1-7])$`
- `version` (mandatory): `^[0-9]+$`

**Optional options**
- `source_code_link` (optional): `^https://github.com/.*`
- `testlink` (optional): `^https://github.com/.*`

### `gd_chklst` — Process Checklist
Parts: `2`

**Mandatory options**
- `status` (mandatory): `^(valid|draft)$`
- `version` (mandatory): `^[0-9]+$`

**Optional options**
- `source_code_link` (optional): `^https://github.com/.*`
- `testlink` (optional): `^https://github.com/.*`

**Optional links**
- `complies` (optional; targets: `std_req`)

### `gd_guidl` — Process Guideline
Parts: `2`

**Mandatory options**
- `status` (mandatory): `^(valid|draft)$`
- `version` (mandatory): `^[0-9]+$`

**Optional options**
- `source_code_link` (optional): `^https://github.com/.*`
- `testlink` (optional): `^https://github.com/.*`

**Optional links**
- `complies` (optional; targets: `std_req`)

### `gd_method` — Process Method
Prefix: `gd_meth__`
Parts: `2`

**Mandatory options**
- `status` (mandatory): `^(valid|draft)$`
- `version` (mandatory): `^[0-9]+$`

**Optional options**
- `source_code_link` (optional): `^https://github.com/.*`
- `testlink` (optional): `^https://github.com/.*`

**Optional links**
- `complies` (optional; targets: `std_req`)

### `gd_req` — Process Requirements
Tags: `requirement`
Parts: `2`

**Mandatory options**
- `content` (mandatory): `^[\s\S]+$`
- `version` (mandatory): `^[0-9]+$`

**Optional options**
- `source_code_link` (optional): `^https://github.com/.*`
- `testlink` (optional): `^https://github.com/.*`

**Optional links**
- `complies` (optional; targets: `std_req`)
- `satisfies` (optional; targets: `workflow`)

### `gd_temp` — Process Template
Parts: `2`

**Mandatory options**
- `status` (mandatory): `^(valid|draft)$`
- `version` (mandatory): `^[0-9]+$`

**Optional options**
- `source_code_link` (optional): `^https://github.com/.*`
- `testlink` (optional): `^https://github.com/.*`

**Optional links**
- `complies` (optional; targets: `std_req`)

### `logic_arc_int` — Logical Interface & Feature Interface View
Tags: `architecture_element`, `architecture_view`
Parts: `3`

**Mandatory options**
- `safety` (mandatory): `^(QM|ASIL_B)$`
- `security` (mandatory): `^(YES|NO)$`
- `status` (mandatory): `^(valid|invalid)$`
- `version` (mandatory): `^[0-9]+$`

**Optional options**
- `source_code_link` (optional): `^https://github.com/.*`
- `testlink` (optional): `^https://github.com/.*`

**Optional links**
- `fulfils` (optional; targets: `feat_req`)
- `included_by` (optional; targets: `feat`)

### `logic_arc_int_op` — Logical Interface Operation
Tags: `architecture_element`
Parts: `3`

**Mandatory options**
- `safety` (mandatory): `^(QM|ASIL_B)$`
- `security` (mandatory): `^(YES|NO)$`
- `status` (mandatory): `^(valid|invalid)$`
- `version` (mandatory): `^[0-9]+$`

**Mandatory links**
- `included_by` (mandatory; targets: `logic_arc_int`)

**Optional options**
- `source_code_link` (optional): `^https://github.com/.*`
- `testlink` (optional): `^https://github.com/.*`

### `mod` — Module
Tags: `architecture_element`
Parts: `2`

**Mandatory options**
- `version` (mandatory): `^[0-9]+$`

**Mandatory links**
- `includes` (mandatory; targets: `comp`)

**Optional options**
- `safety` (optional): `^(QM|ASIL_B)$`
- `security` (optional): `^(YES|NO)$`
- `source_code_link` (optional): `^https://github.com/.*`
- `status` (optional): `^(valid|invalid)$`
- `testlink` (optional): `^https://github.com/.*`

### `mod_ver_report` — Module Verification Report
Tags: `verification_report`
Prefix: `mod_vrep__`
Parts: `3`

**Mandatory options**
- `safety` (mandatory): `^(QM|ASIL_B)$`
- `security` (mandatory): `^(YES|NO)$`
- `status` (mandatory): `^(valid|invalid)$`
- `verification_method` (mandatory): `^.+$`
- `version` (mandatory): `^[0-9]+$`

**Mandatory links**
- `belongs_to` (mandatory; targets: `mod`)

**Optional options**
- `applies_to_module_version` (optional): `^.*$`
- `branch_coverage_percent` (optional): `^(100|[1-9]?[0-9])$`
- `line_coverage_percent` (optional): `^(100|[1-9]?[0-9])$`
- `requirements_tests_completely_passed_percent` (optional): `^(100|[1-9]?[0-9])$`
- `source_code_link` (optional): `^https://github.com/.*`
- `testlink` (optional): `^https://github.com/.*`

**Optional links**
- `contains` (optional; targets: any target (`*`))
- `covers` (optional; targets: any target (`*`))
- `evidence` (optional; targets: any target (`*`))
- `realizes` (optional; targets: `workproduct`)

### `mod_view_dyn` — Module Architecture Dynamic View
Parts: `3`

**Mandatory options**
- `version` (mandatory): `^[0-9]+$`

**Optional options**
- `source_code_link` (optional): `^https://github.com/.*`
- `testlink` (optional): `^https://github.com/.*`

**Optional links**
- `belongs_to` (optional; targets: `mod`)

### `mod_view_sta` — Module Architecture Static View
Tags: `architecture_view`
Parts: `3`

**Mandatory options**
- `version` (mandatory): `^[0-9]+$`

**Optional options**
- `source_code_link` (optional): `^https://github.com/.*`
- `testlink` (optional): `^https://github.com/.*`

**Optional links**
- `belongs_to` (optional; targets: `mod`)
- `includes` (optional; targets: `comp`)

### `plat_saf_dfa` — Platform Dependent Failure Analysis
Parts: `3`

**Mandatory options**
- `content` (mandatory): `^[\s\S]+$`
- `failure_effect` (mandatory): `^.+$`
- `failure_id` (mandatory): `^.+$`
- `status` (mandatory): `^(valid|invalid)$`
- `sufficient` (mandatory): `^(yes|no)$`
- `version` (mandatory): `^[0-9]+$`

**Mandatory links**
- `violates` (mandatory; targets: `feat_arc_sta`)

**Optional options**
- `mitigation_issue` (optional): `^https://github\.com/[^/]+/[^/]+/issues/\d+$`
- `safety_relevant` (optional): `^(yes|no)$`
- `source_code_link` (optional): `^https://github.com/.*`
- `testlink` (optional): `^https://github.com/.*`

**Optional links**
- `mitigated_by` (optional; targets: `aou_req`, `stkh_req`)

### `plat_sec_ana` — Platform Security Analysis (Threat Scenario)
Tags: `security_analysis`, `threat_scenario`
Parts: `3`

**Mandatory options**
- `content` (mandatory): `^[\s\S]+$`
- `status` (mandatory): `^(valid|invalid)$`
- `sufficient` (mandatory): `^(yes|no)$`
- `threat_effect` (mandatory): `^.+$`
- `threat_scenario_id` (mandatory): `^(AS_01_(01|02|04|05|06|07|09|10)|CO_01_0[1-7]|SC_01_0[2-5]|SI_01_0[2-5]|UI_01_(0[1-9]|1[0-2]))$`
- `version` (mandatory): `^[0-9]+$`

**Optional options**
- `mitigation_issue` (optional): `^https://github\.com/[^/]+/[^/]+/issues/\d+$`
- `source_code_link` (optional): `^https://github.com/.*`
- `testlink` (optional): `^https://github.com/.*`

**Optional links**
- `mitigated_by` (optional; targets: `aou_req`, `feat_req`)

### `plat_sec_threat` — Platform Security Analysis Threat (STRIDE)
Tags: `security_analysis`, `stride_threat`
Parts: `3`

**Mandatory options**
- `content` (mandatory): `^[\s\S]+$`
- `status` (mandatory): `^(valid|invalid)$`
- `threat_id` (mandatory): `^(AU_01_0[1-3]|AZ_01_0[1-3]|CT_01_0[1-2]|DS_01_0[1-3]|EX_01_0[1-6]|LA_01_0[1-3]|MT_01_0[1-7])$`
- `version` (mandatory): `^[0-9]+$`

**Optional options**
- `source_code_link` (optional): `^https://github.com/.*`
- `testlink` (optional): `^https://github.com/.*`

### `real_arc_int` — Interface & Component Interface
Tags: `architecture_element`, `architecture_view`
Parts: `3`

**Mandatory options**
- `language` (mandatory): `^(cpp|rust)$`
- `safety` (mandatory): `^(QM|ASIL_B)$`
- `security` (mandatory): `^(YES|NO)$`
- `status` (mandatory): `^(valid|invalid)$`
- `version` (mandatory): `^[0-9]+$`

**Optional options**
- `source_code_link` (optional): `^https://github.com/.*`
- `testlink` (optional): `^https://github.com/.*`

**Optional links**
- `fulfils` (optional; targets: `comp_req`)

### `real_arc_int_op` — Interface Operation
Tags: `architecture_element`
Parts: `3`

**Mandatory options**
- `safety` (mandatory): `^(QM|ASIL_B)$`
- `security` (mandatory): `^(YES|NO)$`
- `status` (mandatory): `^(valid|invalid)$`
- `version` (mandatory): `^[0-9]+$`

**Mandatory links**
- `included_by` (mandatory; targets: `real_arc_int`)

**Optional options**
- `source_code_link` (optional): `^https://github.com/.*`
- `testlink` (optional): `^https://github.com/.*`

**Optional links**
- `implements` (optional; targets: `logic_arc_int_op`)

### `review_header` — Review Header
Parts: `3`

**Mandatory options**
- `approvers` (mandatory): `^.+$`
- `hash` (mandatory): `^.+$`
- `reviewers` (mandatory): `^.+$`
- `template` (mandatory): `^.+$`
- `version` (mandatory): `^[0-9]+$`

**Optional options**
- `source_code_link` (optional): `^https://github.com/.*`
- `testlink` (optional): `^https://github.com/.*`

### `role` — Role
Prefix: `rl__`
Parts: `2`

**Mandatory options**
- `version` (mandatory): `^[0-9]+$`

**Optional options**
- `source_code_link` (optional): `^https://github.com/.*`
- `testlink` (optional): `^https://github.com/.*`

**Optional links**
- `contains` (optional; targets: `role`)

### `std_req` — Standard Requirement
Parts: `3`

**Mandatory options**
- `id` (mandatory): `^std_req__(iso26262|isosae21434|isopas8926|aspice_40)__[0-9a-zA-Z_-]*$`
- `status` (mandatory): `^(valid)$`
- `version` (mandatory): `^[0-9]+$`

**Optional options**
- `source_code_link` (optional): `^https://github.com/.*`
- `testlink` (optional): `^https://github.com/.*`

**Optional links**
- `links` (optional; targets: any target (`*`))

### `std_wp` — Standard Work Product
Parts: `3`

**Mandatory options**
- `id` (mandatory): `^std_wp__(iso26262|isosae21434|isopas8926|aspice_40)__[0-9a-z_]*$`
- `status` (mandatory): `^(valid)$`
- `version` (mandatory): `^[0-9]+$`

**Optional options**
- `source_code_link` (optional): `^https://github.com/.*`
- `testlink` (optional): `^https://github.com/.*`

### `stkh_req` — Stakeholder Requirement
Tags: `requirement`, `requirement_excl_process`
Parts: `3`

**Mandatory options**
- `rationale` (mandatory): `^.+$`
- `reqtype` (mandatory): `^(Functional|Interface|Process|Non-Functional)$`
- `safety` (mandatory): `^(QM|ASIL_B)$`
- `security` (mandatory): `^(YES|NO)$`
- `status` (mandatory): `^(valid|invalid)$`
- `valid_from` (mandatory): `^v(0|[1-9]\d*)\.(0|[1-9]\d*)(\.(0|[1-9]\d*))?$`
- `version` (mandatory): `^[0-9]+$`

**Optional options**
- `hash` (optional): `^.*$`
- `source_code_link` (optional): `^https://github.com/.*`
- `testcovered` (optional): `^(YES|NO)$`
- `testlink` (optional): `^https://github.com/.*`
- `valid_until` (optional): `^v(0|[1-9]\d*)\.(0|[1-9]\d*)(\.(0|[1-9]\d*))?$`

### `tenet` — Tenet
Parts: `3`

**Mandatory options**
- `id` (mandatory): `^tenet__[0-9a-zA-Z_-]*$`
- `status` (mandatory): `^(draft|valid)$`
- `version` (mandatory): `^[0-9]+$`

**Optional options**
- `source_code_link` (optional): `^https://github.com/.*`
- `testlink` (optional): `^https://github.com/.*`

**Optional links**
- `links` (optional; targets: any target (`*`))

### `testcase` — Testcase Needs parsed from test.xml files

**Mandatory options**
- `version` (mandatory): `^[0-9]+$`

**Optional options**
- `derivation_technique` (optional): `^.*$`
- `file` (optional): `^.*$`
- `line` (optional): `^.*$`
- `name` (optional): `^.*$`
- `result` (optional): `^.*$`
- `result_text` (optional): `^.*$`
- `source_code_link` (optional): `^https://github.com/.*`
- `test_type` (optional): `^.*$`
- `testlink` (optional): `^https://github.com/.*`

**Optional links**
- `fully_verifies` (optional; targets: any target (`*`))
- `partially_verifies` (optional; targets: any target (`*`))

### `tool_req` — Tool Requirement
Tags: `requirement`, `requirement_excl_process`
Parts: `2`

**Mandatory options**
- `content` (mandatory): `^[\s\S]+$`
- `version` (mandatory): `^[0-9]+$`

**Optional options**
- `hash` (optional): `^.*$`
- `implemented` (optional): `^(YES|PARTIAL|NO)$`
- `parent_covered` (optional): `^.*$`
- `parent_has_problem` (optional): `^.*$`
- `safety` (optional): `^(QM|ASIL_B)$`
- `security` (optional): `^(YES|NO)$`
- `source_code_link` (optional): `^https://github.com/.*`
- `tags` (optional): `^.*$`
- `testcovered` (optional): `^(YES|NO)$`
- `testlink` (optional): `^https://github.com/.*`

**Optional links**
- `satisfies` (optional; targets: `comp_req`, `feat_req`, `gd_req`, `stkh_req`)

### `tsf` — TSF
Parts: `3`

**Mandatory options**
- `id` (mandatory): `^tsf__[0-9a-zA-Z_-]*$`
- `status` (mandatory): `^(draft|valid)$`
- `version` (mandatory): `^[0-9]+$`

**Optional options**
- `source_code_link` (optional): `^https://github.com/.*`
- `testlink` (optional): `^https://github.com/.*`

**Optional links**
- `links` (optional; targets: any target (`*`))

### `workflow` — Workflow
Prefix: `wf__`
Parts: `2`

**Mandatory options**
- `status` (mandatory): `^(valid|draft)$`
- `version` (mandatory): `^[0-9]+$`

**Mandatory links**
- `approved_by` (mandatory; targets: `role`)
- `input` (mandatory; targets: `workproduct`)
- `output` (mandatory; targets: `workproduct`)
- `responsible` (mandatory; targets: `role`)

**Optional options**
- `source_code_link` (optional): `^https://github.com/.*`
- `testlink` (optional): `^https://github.com/.*`

**Optional links**
- `contains` (optional; targets: `gd_chklst`, `gd_guidl`, `gd_method`, `gd_req`, `gd_temp`)
- `has` (optional; targets: `doc_concept`, `doc_getstrt`)
- `supported_by` (optional; targets: `role`)

### `workproduct` — Workproduct
Prefix: `wp__`
Parts: `2`

**Mandatory options**
- `status` (mandatory): `^(valid|draft)$`
- `version` (mandatory): `^[0-9]+$`

**Optional options**
- `source_code_link` (optional): `^https://github.com/.*`
- `testlink` (optional): `^https://github.com/.*`

**Optional links**
- `complies` (optional; targets: `std_req`, `std_wp`)

## Link types

- `affects` — declared; outgoing `affects`, incoming `affected by`.
- `approved_by` — declared; outgoing `approved by`, incoming `approves`.
- `belongs_to` — declared; outgoing `belongs to`, incoming `has`.
- `complies` — declared; outgoing `complies`, incoming `complies to`.
- `consists_of` — declared; outgoing `consists of`, incoming `forms part of`.
- `contains` — declared; outgoing `contains`, incoming `contained by`.
- `covers` — declared; outgoing `covers`, incoming `covered by`.
- `derived_from` — declared; outgoing `derived from`, incoming `derives`.
- `evidence` — declared; outgoing `evidence`, incoming `evidence_for`.
- `fulfils` — declared; outgoing `fulfils`, incoming `fulfilled by`.
- `fully_verifies` — declared; outgoing `fully_verifies`, incoming `fully_verified_by`.
- `has` — declared; outgoing `has`, incoming `relates to`.
- `implements` — declared; outgoing `implements`, incoming `implemented by`.
- `included_by` — declared; outgoing `included by`, incoming `includes`.
- `includes` — declared; outgoing `includes`, incoming `included by`.
- `input` — declared; outgoing `needs input`, incoming `is input to`.
- `links` — undeclared built-in link type.
- `mitigated_by` — declared; outgoing `mitigated_by`, incoming `mitigates`.
- `output` — declared; outgoing `outputs`, incoming `is output from`.
- `partially_verifies` — declared; outgoing `partially_verifies`, incoming `partially_verified_by`.
- `provides` — declared; outgoing `provides`, incoming `provided by`.
- `realizes` — declared; outgoing `realizes`, incoming `realized by`.
- `responsible` — declared; outgoing `responsible`, incoming `is responsible for`.
- `satisfied_by` — declared; outgoing `satisfied by`, incoming `satisfies`.
- `satisfies` — declared; outgoing `satisfies`, incoming `satisfied by`.
- `supported_by` — declared; outgoing `supported by`, incoming `supports`.
- `uses` — declared; outgoing `uses`, incoming `used by`.
- `violates` — declared; outgoing `violates`, incoming `violated_by`.

## Graph rules

### `tool_req__docs_arch_link_qm_to_safety_req`
Applies to: `comp_arc_sta`, `feat_arc_sta`, `logic_arc_int`, `logic_arc_int_op`, `real_arc_int`, `real_arc_int_op`

Condition:
```text
safety == QM
```

Check:
```text
{
  "fulfils": "safety != QM"
}
```

Explanation: An QM architecture element cannot implement ASIL requirements.

### `tool_req__docs_arch_link_security`
Applies to: `comp_arc_sta`, `feat_arc_sta`, `logic_arc_int`, `logic_arc_int_op`, `real_arc_int`, `real_arc_int_op`

Condition:
```text
security == YES
```

Check:
```text
{
  "implements": "security == YES"
}
```

Explanation: An security architecture element can only link other security architecture elements.

### `tool_req__docs_common_attr_safety_link_check`
Applies to: `aou_req`, `comp_req`, `feat_req`, `gd_req`, `stkh_req`, `tool_req`

Condition:
```text
safety == QM
```

Check:
```text
{
  "derived_from": "safety == QM"
}
```

Explanation: QM requirements cannot be derived from ASIL requirements.

### `tool_req__docs_req_arch_link_safety_to_arch`
Applies to: `comp_arc_sta`, `feat_arc_sta`, `logic_arc_int`, `logic_arc_int_op`, `real_arc_int`, `real_arc_int_op`

Condition:
```text
{
  "and": [
    "safety != QM",
    "status == valid"
  ]
}
```

Check:
```text
{
  "implements": {
    "and": [
      "safety != QM",
      "status == valid"
    ]
  }
}
```

Explanation: An safety architecture element can only link other safety architecture elements.

### `workproduct_aspice_40`
Applies to: `workproduct`

Condition:
```text
complies != []
```

Check:
```text
{
  "complies": {
    "or": [
      "id contains aspice_40__iic",
      "id contains std_wp"
    ]
  }
}
```

Explanation: Workproducts may only link to ASPICE 40 IIC stakeholder requirements. Please ensure that the linked requirement is an ASPICE 40 IIC stakeholder requirement.

## Prohibited words

- `content_check` checks `content` for `requirement_excl_process`: `about`, `absolutely`, `just`, `really`, `some`, `thing`.
- `title_check` checks `title` for all tags: `must`, `shall`, `will`.
